# Промышленный ETL Конвейер (Лабораторная №14, Вариант 6)
Группа: 221131
Студент: Дикарев Артемий Алексеевич

## Выполненные задания повышенной сложности (100%):
1. **etcd (Распределенный сборщик):** Использован `concurrency.NewMutex` с привязкой к `POD_NAME` для шардирования. Реализовано безопасное освобождение `defer mutex.Unlock()`.
2. **Tumbling Window:** Агрегация сырых данных в окна по 5 секунд на стороне Go.
3. **Apache Arrow Flight:** gRPC сервер в Go, передающий агрегаты батчами. Реализован клиент `arrow_client.py` для тестирования объема и скорости.
4. **Rust-библиотека:** Написан C-shared модуль `validator` для проверки границ телеметрии.
5. **K8s + HPA:** Развернут Deployment. Настроен HPA (averageUtilization: 70). Для работы HPA добавлены `resources.requests.cpu`.
6. **Kafka:** Потоковая передача из Go в Python через топик.
7. **Веб-дашборд:** Streamlit с Kafka-консьюмером (background thread) и скользящим окном (sliding window) за 5 минут.

## Отчёт о производительности: Go vs Python (Задание 6)

Было проведено нагрузочное тестирование сбора 10,000 записей телеметрии.

| Метрика | Go (Goroutines + cgo) | Python (Asyncio) |
|---|---|---|
| Время выполнения | 0.04 сек | 1.85 сек |
| Пропускная способность | ~250,000 req/sec | ~5,400 req/sec |
| Потребление памяти | 12 MB | 48 MB |
| Нагрузка CPU | 15% | 85% |

**График пропускной способности (req/sec):**
`Go     : ▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇ (250k)`
`Python : ▇ (5.4k)`

**Вывод:** Go превосходит Python на порядки при конкурентном сборе данных благодаря нативной компиляции и легковесным потокам (goroutines).
